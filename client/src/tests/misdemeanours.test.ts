import Misdomeanours from "../components/misdemeanours";
describe("Testing the fetch", () => {
  test("Check that the fetch returns 3 entries", () => {
    expect("").toBe("");
  });
  test("Check that the entries for citizen ID are valid", () => {
    expect("").toBe("");
  });
});
export {};
